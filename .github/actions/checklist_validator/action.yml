name: "Checklist Validator"
description: "Fails PR if any checkboxes are unchecked"

inputs:
  body:
    description: "The PR body text"
    required: true

runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.body }}
